import random
class enemy:
  def __init__(type, hp, damge, attack1, attack2):
    hpp = random.randint(hp-3,hp+5)
    damages = random.randint(damage+5,damage+5)
    attackone.name = attack1
    attacktwo.name = attack2
